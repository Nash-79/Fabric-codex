# Git hooks are shell scripts — CRLF in the shebang line breaks them.
.githooks/* text eol=lf

# Force SVG and JSON files to use LF line endings so hashes match across platforms
*.svg text eol=lf
*.json text eol=lf
